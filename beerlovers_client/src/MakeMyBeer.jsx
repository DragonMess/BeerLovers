import React from "react";
import useApplication from "./hooks/useApplicationData";
import Cart from "./Cart";
const MakeMyBeer = () => {
  const { state, setState } = useApplication();

  return <div></div>;
};
export default MakeMyBeer;
